import type{ReactNode}from'react';export const Panel=({title,children}:{title:string;children:ReactNode})=><section className="panel"><h2>{title}</h2>{children}</section>;export const Select=({value,onChange,options,blank}:{value:string,onChange:(v:string)=>void,options:string[],blank?:string})=><select value={value} onChange={e=>onChange(e.target.value)}>{blank!==undefined&&<option value="">{blank}</option>}{options.map(o=><option key={o}>{o}</option>)}</select>;export const Empty=({text}:{text:string})=><div className="card muted">{text}</div>;