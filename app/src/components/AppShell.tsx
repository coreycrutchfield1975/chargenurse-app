import type { ReactNode } from 'react';
export type AppPage='dashboard'|'veterans'|'appointments'|'calendar'|'transport'|'treatments'|'staff'|'morning-report'|'managed-lists';
const navItems:Array<{label:string;page:AppPage}>=[['Dashboard','dashboard'],['Veterans','veterans'],['Appointments','appointments'],['Calendar','calendar'],['Transport','transport'],['Treatments','treatments'],['Staff Assignments','staff'],['Morning Report','morning-report'],['Managed Lists','managed-lists']].map(([label,page])=>({label,page:page as AppPage}));
export function AppShell({activePage,onNavigate,children}:{activePage:AppPage;onNavigate:(p:AppPage)=>void;children:ReactNode}){return <div className="app-shell"><header className="app-header"><div><h1>BRAVOSHIFT</h1><p>Nurse CommandPost Center</p><span>Caring for those on Libertyville CLC</span></div><div className="version-badge">v2 · v1.8 React Port</div></header><div className="phi-warning">Static browser build. Do not enter real Veteran information or PHI in a public development environment.</div><nav className="top-nav" aria-label="Primary navigation">{navItems.map(i=><button key={i.page} className={i.page===activePage?'active':''} type="button" onClick={()=>onNavigate(i.page)}>{i.label}</button>)}</nav><main>{children}</main></div>}
