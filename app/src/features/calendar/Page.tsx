import type{Props}from'../../App';export default function Page({db}:Props){const groups=db.appointments.reduce<Record<string,typeof db.appointments>>((m,a)=>{(m[a.date]??=[]).push(a);return m},{});return <><h2>Calendar</h2><div className="calendar">{Object.entries(groups).sort(([a],[b])=>a.localeCompare(b)).map(([d,items])=><div className="panel" key={d}><h3>{d}</h3>{items.map(a=><div className="card" key={a.id}>{a.time} — {db.veterans.find(v=>v.id===a.vetId)?.name} — {a.reason}</div>)}</div>)}</div></>}