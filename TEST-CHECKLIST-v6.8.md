# ChargeNurse v6.8 Test Checklist

- Open each primary page and confirm it loads without a broken background.
- Select each built-in image and navigate between pages; selection must persist.
- Enter an invalid project image path; the page must restore its default and show a brief warning.
- Change overlay, panel opacity, fit, and position; settings must persist.
- Upload a JPG/PNG/WEBP and confirm it remains browser-local.
- Reset to Page Default and confirm inline page themes still work.
- Print each page and confirm backgrounds/appearance controls do not interfere.
- Enable reduced motion at OS/browser level and confirm fade/hover motion is removed or reduced.
